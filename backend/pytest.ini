[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
testpaths = accounts battles problems
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning

asyncio_mode = auto
markers =
    cpp: exercises the C++ toolchain (skipped when compilation is pathologically slow)
